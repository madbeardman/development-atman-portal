# Atman-portal

Simple nodejs application designed for use with Tabletop RPG's.

API:
>> POST /characters

>> GET /characters

>> GET /characters/:id

>> DELETE /characters/:id

>> PATCH /characters/:id

>> POST /users

